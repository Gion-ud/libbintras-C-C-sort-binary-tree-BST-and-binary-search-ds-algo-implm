-- libbintras project --
In this project I will implm a BST with pool allocator, iterator and
custom pluggable comparators as a container, sorted data structure like
sorted arr of kv, binary search, argsort/indexsort and some sort algo like
insertion_sort.
i will prob also implm other ds like stack or wrapping them in cxx template
classes.
